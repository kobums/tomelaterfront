# Frontend (To Me, Later)

`To Me, Later`의 프론트엔드 프로젝트입니다. React와 Tailwind CSS로 구축되었습니다.

## 🛠 설치 및 실행 (Setup & Run)

### 사전 요구사항
- Node.js (v18 이상 권장)
- npm

### 1. 패키지 설치
프로젝트 루트(`to_me_a_year_later`)가 아닌 `front` 디렉토리로 이동하여 실행합니다.

```bash
cd front
npm install
```

### 2. 개발 서버 실행
```bash
npm run dev
```
실행 후 터미널에 표시되는 로컬 주소(예: `http://localhost:5173`)로 접속합니다.

### 3. 빌드 (Production Build)
```bash
npm run build
```
빌드된 결과물은 `dist` 디렉토리에 생성됩니다.

## 📁 디렉토리 구조
```
front/
├── public/          # 정적 파일 (favicon, 로고 등)
├── src/
│   ├── components/  # 재사용 가능한 UI 컴포넌트
│   ├── pages/       # 페이지 단위 컴포넌트
│   ├── services/    # API 통신 로직 (auth.service.ts 등)
│   ├── store/       # 전역 상태 관리 (zustand)
│   ├── util/        # 유틸리티 함수
│   ├── App.tsx      # 메인 앱 컴포넌트 및 라우팅
│   └── main.tsx     # 진입점
└── index.html       # HTML 템플릿
```

## 🎨 주요 라이브러리
- **React**: UI 구축
- **Tailwind CSS**: 스타일링
- **Axios**: HTTP 클라이언트
- **Zustand**: 상태 관리
- **React Router DOM**: 라우팅
